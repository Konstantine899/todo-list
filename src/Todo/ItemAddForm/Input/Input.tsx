import React from "react";

const Input = () => {
  return (
    <input
      type="text"
      className="form-control"
      placeholder="Что-то нужно добавить"
    />
  );
};

export default Input;
